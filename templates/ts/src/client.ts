// Client-side customization hooks
// If you don't need any customization you may delete this file.

// Initialization before the client starts.
// If you return a promise Rakkas will wait until it resolves.
export function beforeStartClient(): void {
	// console.log("Hello world!");
}
