describe("Sample unit test", () => {
	// @testing-library/react is also available for component testing
	it("adds two and two correctly", () => {
		expect(2 + 2).toBe(4);
	});
});

// Must be a module
export {};
